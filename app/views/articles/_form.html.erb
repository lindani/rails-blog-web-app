<%= form_with model: data do |form| %>
  <% if data.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(data.errors.count, "error") %> prohibited
        this data from being saved:
      </h2>
      <ul>
        <% data.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>